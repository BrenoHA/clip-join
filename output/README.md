# output/

This folder holds the videos ClipJoin creates when you join clips. Nothing else
lives here. Each join is named with a timestamp by default (e.g.
`joined_output_2026-07-16_114530.mp4`), so repeated joins never overwrite one
another.
